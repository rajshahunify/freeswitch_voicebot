# Core dependencies
fastapi
uvicorn[standard]
websockets<14.0

# FreeSWITCH ESL
greenswitch

# Audio processing
numpy<2.0.0
torch
torchaudio

# DeepFilterNet2
deepfilternet

# HTTP requests
requests

# Intent matching (LEGACY — kept for backward compatibility)
fuzzywuzzy
python-Levenshtein
# sentence-transformers  # REMOVED — replaced by LLM, saves ~1GB image size

# Utilities
python-multipart

# Audio resampling (CRITICAL for improved NC)
resampy

redis

# ──── NEW: AI Pipeline Dependencies ────

# STT — local Faster-Whisper (only needed when STT_PROVIDER=local)
faster-whisper

# TTS — Edge-TTS (free, no API key)
edge-tts

# LLM — Gemini API client (NEW SDK — old google-generativeai is deprecated/broken)
google-genai

# Audio format conversion (MP3→WAV for Edge-TTS output)
pydub
